# Prometheus 配置文件 — SakikoMind 智能客服系统

global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'sakikomind'
    environment: 'production'

rule_files:
  - "alerts/*.yml"

scrape_configs:
  # SakikoMind 主应用指标
  - job_name: 'sakikomind-app'
    static_configs:
      - targets: ['sakikomind:8000']
        labels:
          service: 'sakikomind'
    scrape_interval: 10s
    metrics_path: '/metrics'

  # Prometheus 自监控
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
